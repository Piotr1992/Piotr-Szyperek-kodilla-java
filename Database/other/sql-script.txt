


SELECT U.FIRSTNAME FIRSTNAME, U.LASTNAME LASTNAME
FROM ISSUES I JOIN USERS U ON U.ID = I.USER_ID_ASSIGNEDTO
GROUP BY U.LASTNAME, U.FIRSTNAME
HAVING COUNT(*) >= 2;

select * from issues;





UPDATE issues SET USER_ID_ASSIGNEDTO=5 WHERE ID IN (3, 5);
COMMIT;
UPDATE issues SET USER_ID_ASSIGNEDTO=4 WHERE ID=2;
COMMIT;
